<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-3">
            <a class="text-blue-700" href="/">back to home</a>
        </div>
        <h2 class="text-3xl tracking-tighter mb-3">All User</h2>
        <div class="users flex flex-wrap gap-3">
            <% if(users.length> 0){ %>
                <% users.forEach(function(user){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-52 rounded-lg  bg-zinc-700  overflow-hidden">
                            <img class="w-full h-full object-cover object-center" src="<%= user.image %>">
                        </div>
                        <h3 class="text-2xl mt-3 tracking-tighter">
                            <%= user.name %>
                        </h3>
                        <h5 class="text-zinc-500">
                            <%= user.email %>
                                </h3>
                                <div class="flex gap-3 mt-10 justify-between">
                                    <a class="text-zinc-300" href="/edit/<%= user._id %>">Edit</a>
                                    <a class="text-red-300" href="/delete/<%= user._id %>">Delete</a>
                                </div>
                    </div>
                    <% }) %>
                <% } else{ %>
                        <h3 class="text-2xl text-zinc-200 mt-10">No record</h3>
                    <% } %>
        </div>

    </div>
</body>

</html>